# 线A/线B整合规划

> 2026-09-18。背景：库管Excel清洗管道的P2设计(《P2_数据库设计.md》《P2_清洗与匹配规则.md》，下称"线A")和
> haidilao-receiving-app的Postgres后端(供应商/物料/收货/发票/Credit/ERP入库单/三方匹配/OCR，下称"线B")，
> 是两条独立发展、没有真正当成同一个数据模型看待过的线。这份文档是Claude+ChatGPT多轮深度讨论
> (用户明确要求"多思考多质疑")后，把两条线理成一条的结论，供后续实施参考。

---

## 一、核心决定：以线B为唯一正式数据库

线A"以后进飞书多维表、再以后考虑定制网页系统"这条落地路线**作废**。理由：

- 线B已经是真实部署上线的Postgres数据库，不是设计稿
- 线B在关键设计点上比线A原方案更成熟：
  - 发票↔收货/入库单用真正的多对多关联表(`invoice_receiving_links`/`invoice_erp_receipt_links`)，
    不是线A原设计的单值外键(线A那种设计挡不住"一张发票合并覆盖多次送货"这种真实场景)
  - 物料名称匹配用状态机(`supplier_material_mapping`：pending/active/superseded/disputed/discarded)，
    比线A"静态待确认表，人工填一次就完事"更适合"新单据持续进来、匹配规则要不断学习"这种场景
  - 已经有真实可用的OCR解析引擎(Claude vision API)，线A完全没有涉及这块
- 再造一套飞书表，等于把已经跑通的匹配/审计/OCR能力重新做一遍，纯浪费

**线A的价值收窄为**：清洗用户手头的历史Excel数据(8月发票校验总表等)，**目标产出从"飞书多维表"改成"能导入线B这个Postgres的结构化数据"**。线A文档里"物料匹配5级优先级"、"任何非精确匹配不直接写正式表"这套规则依然有效、直接复用，只是最终写入目标换了。

---

## 二、线B缺的两块，要补

线B目前只记录"货/单已经存在之后"的信息(收货、发票、ERP入库单)，缺两块线A认为必需、且经用户确认真实存在的环节：

### 2.1 叫货记录(简化版，不是完整PO)

**用户确认的真实业务**："全部都是我叫货了才送"——每次供应商送货前，用户都会先报一次数量，不是纯被动按配送周期收货。

但线A原设计的采购单(状态机：草稿/已下单/部分到货/已到货/已入库/已开票/已付款/已关闭)对这家单店过度设计——没有"供应商确认/审批"这种正式环节，报了就等送。

**改用简化版"叫货记录"表**，不建状态机：

| 字段 | 说明 |
|---|---|
| 叫货记录ID | 主键 |
| 叫货日期 | |
| 供应商ID | 关联供应商 |
| 明细 | 物料+叫货数量(子表，一条叫货记录对多个物料) |
| 备注 | |
| 关联收货记录 | 到货后回填，关联到`receiving_records`，用于核对"叫的和到的是否一致"——这是叫货记录存在的主要价值，不是走流程 |

### 2.2 库存流水表——如实承认自己不是"唯一真相"

**用户确认的真实情况**："有一部分物料每周会统计一次(盘点)，其他的我这里没有什么数据，如果后面发现有内部库存流水的，我再添加。"

海底捞内部系统本身可能有权威库存数据，但用户目前拿不到/没对接。所以这张表**不能假装是线A文档说的"全系统库存变化的唯一真相"**——出库/消耗方向根本没有可靠数据源。

设计原则：

1. **入库方向如实记录**：采购/收货/入库单已经有可靠数据源(线B已有)，持续记流水
2. **出库/消耗方向不装**：目前唯一能拿到的是周期性盘点(部分物料，每周)，不是逐笔消耗记录。消耗量只能靠"两次盘点之间的入库量+期初盘点-期末盘点"倒算出一个区间估计，不是真实的逐笔出库流水
3. **"当前库存"这个数字必须标注是估算值**：对有盘点的物料，是"上次盘点数+盘点后的入库量"，UI上要清楚写"估算，基于N天前的盘点"，不能假装是实时准确库存。没有盘点的物料，压根不提供"当前库存"这个数字，避免给假象
4. **表结构留"数据来源"字段**：盘点/入库单/(预留)未来内部系统API——以后真的接上内部系统的库存数据，不用改表结构，加一种来源类型即可

---

## 三、周盘点"协助填写"——具体功能

用户明确要求："后面还想协助我填写这个每周统计"。按讨论出的优先级(已跟用户对齐)：

1. **到点提醒**：哪些物料这周该盘点了(基于配置好的"每周盘点物料清单")
2. **预设默认值**：填写界面按"上次盘点数+这期间入库量"这个**简单加减法**(不是机器学习预测——问过用户，用户对ML预测感兴趣，但单店规模+每周频率的数据量级现阶段不够训练出有效模型，属于冷启动没数据比简单算法还不准的经典场景，结论是先用简单算法跑几个月攒够真实历史数据，之后再评估要不要升级成ML预测)，预先带出参考值，用户只需要改动跟实际不一样的地方
3. **差异过大提示**：填的数字和预设默认值差太多(比如预计50填了5)，跳出来提示确认一下，防止手滑
4. **(远期可选)语音/拍照转数字**：复用已经在用的Claude vision API技术(跟发票OCR同一套思路)，识别手写盘点表或语音报数——优先级最低，等前3项跑稳了再考虑

---

## 四、下一步

- [x] 设计"叫货记录"表的具体字段字典 + 迁移文件——2026-09-18完成，见`server/db/migrations/0006_order_requests.sql`(`order_requests`+`order_request_items`)+`0007_receiving_order_request_link.sql`(`receiving_records`新增可空`order_request_id`外键)。本地commit `831fabb`，**未push**。
- [x] 设计"库存流水表"的具体字段字典 + 迁移文件(区分数据来源字段)——2026-09-18完成，见`0008_stock_ledger.sql`。只记入库方向(quantity恒正CHECK约束)，`source_type`目前只有`receiving`/`erp_receipt`两种，`UNIQUE(source_type,source_table,source_id)`防止同一来源行被重复计入。
- [x] 设计"周盘点"相关表——2026-09-18完成，见`0009_stocktake.sql`：`stocktake_material_config`(哪些物料需要盘点的配置清单)+`stocktake_records`(盘点绝对数量快照，带`predicted_quantity`字段留痕预设值vs实际填写值)+`stock_estimated_current`视图(最近一次盘点+之后入库流水之和，没盘点过的物料不出现在视图里，不给假数字)。
  - 设计分歧点：库存流水(delta)和盘点快照(绝对数)要不要混进同一张表同一个字段——跟ChatGPT深入讨论后确认**拆成两张表**更经得起长期维护(同一字段两种语义容易被后来的人/AI误用，比如把snapshot当delta加总)。
  - 本地Postgres(`haidilao-pg`容器)从0001-0005真实起点测试通过：外键/UNIQUE/CHECK约束、视图计算结果(30+20=50)、"未盘点物料不出现在视图"均验证正确，测试数据已清理。
- [x] 线A历史Excel数据扫描（清洗前置阶段，按P2_清洗与匹配规则.md要求先扫描后清洗）——2026-09-18完成，见`P2_设计文档/P2_历史数据扫描报告.md`。**重要发现**：`盘点/`目录下有海底捞内部系统每月人工导出的库存盘点表(含"消耗量"字段，760行真实数据)+"日均使用量"表，这可能意味着`0008_stock_ledger.sql`"出库方向没有可靠数据源"这个设计前提不完整（是"没有持续实时数据源"，不是"完全没有数据源"——每月静态快照也是一种数据源，只是频率低，ChatGPT复核认为这点值得用户重新审视，不代表0008设计错了，只是信息不完整时做的判断）。历史范围内共约65-70个供应商发票PDF+多份发票校验总表xlsx(8月216行真实数据)+炒料原料统计表，尚未清洗，等用户对报告里第五节"需要用户决定的事"（清洗范围/优先级/0008是否要调整/两处疑似重复文件）给出方向后才正式开始清洗。
- [x] 采纳上一条发现，用户明确回复"改"——2026-09-18完成，见`server/db/migrations/0010_stocktake_official_import.sql`。**没有改`0008`/`0009`本身**(已应用的migration不原地改，铁律)，新建独立表`stocktake_official_import`承接海底捞月度正式盘点导出，跟`stocktake_records`(员工每周手动快速盘点)刻意分开——两者是不同粒度/权威级别的盘点事件，混一张表容易让以后查询的人搞混语义(讨论过共用一张表加字段区分的方案，ChatGPT复核后判定拆表更清晰)。
  - 关键发现：实测验证`消耗量 = 库存数量(期初) - 总盘点数量`这个等式在757行有完整数据的行里**100%精确成立，0个例外**——消耗量是纯派生值。即便如此仍然存储`consumption_quantity`原样(不只存公式)：一是延续P1"原始字段永远保留"的规矩，二是万一海底捞系统未来算法口径变了，当时真实导出的值才是历史真相。
  - 字段设计：`material_id`允许NULL(这批历史数据还没做物料匹配清洗，不能强行外键约束)，`haidilao_material_code`(海底捞内部物料编码，NOT NULL，原始值永久保留)，`department_breakdown`用jsonb存11个部门/库位分列数量(而非展开成固定列，因为不确定部门清单跨月份是否稳定)。
  - `stock_estimated_current`视图**暂不接入**这张新表——现在material_id大多是NULL，等物料匹配清洗完成、能可靠关联到SKU之后再评估要不要让视图同时查两张盘点表取较新的一次。
  - 本地Postgres(`haidilao-pg`容器，从0001-0009真实起点)测试通过：migration正确应用；真实数据形状插入成功(material_id为NULL的历史行+material_id已匹配的行两种场景都验证)；material_id外键约束、haidilao_material_code非空约束均正确拒绝坏数据；jsonb字段可正确查询；消耗量算术关系验证成立；测试数据已清理，库里不残留任何测试行。
  - 本地commit（未push）：见下方commit hash。
- [x] 线A历史数据真正开始清洗(用户明确说"继续做历史数据清洗")——2026-09-18完成第一批，两个数据源：
  1. **8月发票校验总表.xlsx → `invoices`表**：脚本`server/db/seed/import_p2_invoices_aug2026.py`。158行真实发票，149条精确匹配写入(29个供应商sheet中27个跟`suppliers`表精确字符串匹配，含"Coworkc"/"Cowrock"这种查表后确认是两个独立真实供应商、不是拼写变体的情况)，9条进pending不写正式表：2条因供应商sheet名("丰收"/"HAC")在suppliers表无精确匹配，7条因金额为负数且源数据无退货/信用标注(按P2_清洗与匹配规则.md强制原则，不自行判断转Credit Note，留给用户)。pending清单在`库管数据/P2_8月发票_pending清单.json`(含真实金额，不进git)。**GST字段含义是推断出来的、已标注为假设**：Invoice Amount=含税总额，GST=其中的税额部分(不是额外加的税)——依据抽样数据反推+符合"澳洲食材类供应商大部分商品免GST"的常见业务模式，已跟ChatGPT讨论确认，但不是从源文件明确标注查到的事实，值得用户有空时口头确认一下。
  2. **盘点/7月/附表2：AU6D-20260731库存盘点表.xlsx → `stocktake_official_import`表**：脚本`server/db/seed/import_p2_stocktake_official_jul2026.py`。757行全部精确插入，0条跳过。`material_id`全部保持NULL(haidilao_material_code是海底捞内部系统自己的物料编码，跟P1 SKU的匹配是独立一块清洗工作，这次没做，原始编码/名称原样保留供以后匹配用)。`department_breakdown`字段：源表头结构是2行合并表头、同一批部门/库位标签在两处位置重复出现，语义没有完全理清楚，没有勉强猜字段名——按"列号+原始表头文本+值"原样存成jsonb list，不丢信息也不编错。
  - 两个脚本均在本地Postgres(`haidilao-pg`容器)真实跑过、用独立SQL查询验证过行数/金额汇总(不是只看脚本自己打印的数字)。本地commit `a257ec2`，**未push**。
  - **未完成**：约65-70个供应商发票PDF(需要OCR，本轮没做，`ocrParsingService.js`理论上能直接复用但需要写一个历史批量调用的脚本包装，是独立工作量)、炒料原料统计表.xlsx(跟E3生产工单/成本核算相关，本轮没碰)、7月发票校验总表/各供应商单独xlsx(跟8月文件可能有重复关系，没有逐一核对)、`invoices`表149条记录的`invoice_items`明细行(源数据本身只有发票头信息，没有逐行商品明细，这批数据结构上就到不了item级别)、pending清单里9条记录的后续处理(等用户看完给方向)。
- [x] **7月供应商Invoice PDF批量OCR导入**——2026-09-18完成第一批：`7月/2026.7.6d/`下25个供应商文件夹，每个文件夹里文件名含"Invoice"的PDF(不含Statement对账单，对账单目标表是`supplier_statements`，另开一批处理)。脚本`server/db/seed/import_p2_july_invoices_ocr.mjs`，直接复用`server/services/ocrParsingService.js`的`parseInvoiceDocument`，没有重写OCR逻辑。
  - 供应商匹配：用文件夹名精确匹配`suppliers.name`(文件夹名视同用户自己认定的标准名称，属于精确匹配层级)。25个文件夹全部精确匹配成功，0条进pending。
  - 结果：**24条成功写入`invoices`+`invoice_items`，1条OCR失败**(SKYJ供应商，`领鲜 7月 Invoice.pdf`——模型返回的结构化结果缺少必要字段，重试一次仍然失败，可能是文档渲染问题，需要人工看一下这份PDF本身是否有质量问题)。独立SQL验证：`invoices`表`source='p2_historical_ocr'`的记录数=24，对应`invoice_items`=84行，金额汇总$18,628.79，均与脚本打印一致。
  - `invoice_no`唯一性：全部24条与已有数据(含8月批次149条)无冲突。
  - 本地commit：见下方commit hash。**仍未push、未碰生产库。**
- [x] **两处疑似重复文件核实**：
  - `炒料原料统计表.xlsx`(库管根目录，2026-09-02) vs `盘点/炒料原料统计表.xlsx`(2026-07-01)：**sha256不同、内容不同**(不是简单重复)，但sheet结构完全一致(Sheet1-4维度相同)。根目录版本修改时间晚2个月，**推断是更新过的版本，根目录版本更可能是权威版**——这是基于修改时间的推断，不是逐格比对内容差异确认的，供用户参考。
  - `仅复制-4月发票校验总表.xlsx`：项目里**没有另一份"4月发票校验总表.xlsx"可以比对**——这不是真正的重复文件问题，只是命名带"仅复制"前缀但没有对应的"正式版"存在，可以直接当4月的唯一数据源用。
  - **新发现的疑点**(原扫描报告没找到)：`7月/7月发票校验总表.xlsx`与`7月/7月发票校验总表带BNE.xlsx`——文件名暗示后者"多了BNE"，但实际对比两份文件的**sheet列表完全相同**(都是25个供应商sheet，都已经有"B&E"这个sheet)，真正的差异是其中一个sheet的名字："Coworkc" vs "Cowrock"(这两个是suppliers表里两个不同的真实供应商，不是拼写变体)。"带BNE"这个文件名看起来是误导性的/或者指的是sheet内部某处内容而非sheet列表本身，具体两份文件对应sheet的行内容是否有差异**没有逐行核对**，需要之后清洗7月发票校验总表数据时专门处理，本轮只做了结构层面的发现。
- [x] **炒料原料统计表.xlsx schema定位**：打开确认是"AU6D X月炒料加工产品原材料和辅料统计表"，108行×59列的宽表，品名+多个产品列的原料用量矩阵——**是配方/BOM性质的数据，不是交易记录**，当前migrations 0001-0010没有任何表能承接这种数据(最接近的是ERP设计文档里提到但还没建的"E3生产工单/炒料成本核算"模块)。**结论：不强行塞进现有表，这是一张全新表(配方/BOM表)的设计需求，留给用户决定是否现在需要做，不是这批清洗能顺手带上的**。
- [x] **8月供应商Invoice PDF批量OCR导入，带去重合并**——2026-09-18完成：`AU6D8月/`下27个供应商文件夹(排除SUPAGAS无独立发票文件)，脚本`server/db/seed/import_p2_august_invoices_ocr.mjs`(复用`ocrParsingService.js`，不重写OCR逻辑)。因为8月发票校验总表.xlsx已经把这批发票的表头导入过(149条，无明细行)，这批PDF本质是同一批发票的原始单据，专门做了去重合并：
  - 按`(supplier_id, invoice_no)`匹配到已有表头记录、且该记录当前0条`invoice_items`的 → **不新建发票，把OCR识别出的明细行直接补挂到已有表头上**：7条(Coles/Funglea/JFC/Reward/Unesco/珍珠红/老北方)
  - 完全没有匹配到已有记录的 → 当全新发票插入(表头+明细)：12条
  - 供应商文件夹名在`suppliers`表无精确匹配 → pending，7条(DISCOUNT SOLUTIONS/FRIENDSHIP/FUJA/HAC/Sunstae/YUENS，大概率是大小写/命名差异，不是没有这个供应商)
  - OCR结构化解析失败 → 2条(CFC/SKYJ，报错信息和7月SKYJ那次一样"缺少必要字段，可能是文档渲染问题")
  - 独立SQL验证：`invoices`表`source='p2_historical_ocr'`总数=36(7月24+8月12，与两批脚本各自输出加总一致)
  - 本地commit `33d7936`，`git log origin/main..HEAD`确认5个commit全部未push、未碰生产库

- [x] **前端"录单工作台"UI核心流程**——2026-09-18完成第一版：`src/components/WorkbenchPage.tsx`，挂载在`/workbench`路径(`main.tsx`按路径名分发，跟现有`/overview`用同一套模式，没有引入新的路由库)。核心流程：首页(待录单/待匹配/待Credit录入/异常四块汇总，复用已有的`GET /api/workbench`)→发票列表(按状态筛选)→点开一张→AI识别(OCR)/查找匹配候选/人工确认匹配/确认完成，一条链走完；上传发票走弹窗选供应商+选文件。UI上OCR识别置信度会明确标成"AI识别结果，非人工核实，供参考"，不假装是确定事实。
  - 新增一个小接口`GET /api/workbench/suppliers`(供应商全量列表，供上传时选择用，之前没有任何路由暴露这个)。
  - **本轮范围内没做**：Credit单/供应商对账单(supplier_statements)/审计日志的独立UI页面、手动逐项录入发票明细(目前只有AI识别路径，没做人工回填表单)——核心Invoice流程先做扎实，其余留后续。
  - 实测验证：`npx tsc --noEmit`0错误，`npx vite build`构建成功(257KB/gzip 80KB)，**本地起了一次真实服务器**(`node server/index.js`，`DATABASE_URL`指向本地`haidilao-pg`)，`curl`验证`GET /workbench`返回200(页面能访问到)、`GET /api/workbench`和`GET /api/invoices`未登录返回401(证明登录校验生效，不是漏了鉴权)。测试完已停掉本地服务器进程。
  - **这一批是在"fork worker不能再嵌套开fork"报错之后，直接在当前会话里做的，没有走ChatGPT复核**(llm-handoff连接中途断了，跟receiving-app项目memory里记录过的断线模式一样)——UI设计决策本身比较保守(复用现有代码风格/路由方式，没有引入新框架)，风险不高，但用户如果想让ChatGPT补一轮审视这版UI，下次可以专门再过一轮。
  - 本地commit：见下方commit hash，**未push、未碰生产库**。
- [x] **发现并补上出入库/领用/报损/调拨历史流水数据**——2026-09-18新会话完成：打开`盘点/4月`~`盘点/7月`目录下之前只当"未清洗待办"记了一句话、没实际打开看过的PDF文件后发现，这些是海底捞内部ERP系统导出的纯文本单据(不是扫描图片，不需要OCR，直接用pymupdf提取文字)，包含真实的逐条出入库/领用/报损/调拨数据——这进一步说明`0008_stock_ledger.sql`"出库方向没有可靠数据源"这条设计前提不完整(不是完全没数据，是没有持续实时数据源；这次发现的是月度批量导出的单据级数据，比`stocktake_official_import`发现的月度快照更细，是接近逐笔的操作记录)。
  - 新建`server/db/migrations/0011_internal_movement_import.sql`：`internal_movement_import`表，跟`stock_ledger`/`stocktake_official_import`同样的原则——不改已应用的0008(不能原地改)，新建独立历史归档表；`quantity`字段按源文件原样存储不做符号二次解读(出入库/领用单据恒正，调拨单据保留源文件自带的符号，示例是`-10.000`)；`direction`(in/out)是从单据自身的字段名机械推导(源文件"反冲数量"列=入库，"实发数量"列=出库/领用)，不是人工判断；`haidilao_material_code`是内部编码不是P1 SKU，`material_id`允许NULL等以后匹配清洗。
  - 导入脚本`server/db/seed/import_p2_internal_movement_pdfs.py`：22个源文件(4月6个/5月5个/6月5个/7月6个，含标准出入库领用格式20个+报损明细1个+调拨明细1个两种特殊版式)，纯文本提取+正则/状态机解析，不用OCR。**本地Postgres真实跑通，独立SQL查询验证**：305条记录(加工领用入库单33条/加工领用出库单96条/物料领用单174条/报损1条/调拨1条)，按movement_type+direction汇总的数量合计跟源文件抽样核对一致。
  - **过程中真实发现并修复的一个bug**(不是设计阶段能预见的，是实测才发现)：`盘点/4月/AU6D 4月员工餐.pdf`这一份文件内部实际拼接了两个独立凭证(4925686957页1-4 + 4925688347页5-7)，各自从"序号=1"重新起数——第一版脚本把去重键设成`(source_file, line_no)`且line_no直接取源文件自己的序号，导致跨凭证的序号1/2/3互相被`ON CONFLICT DO NOTHING`当成重复行悄悄丢弃(49条解析出来只剩26条插入库，且voucher_no字段整份文件被错误地固定成第一个凭证号，没有随第二个凭证更新)。修复：line_no改成文件级全局递增计数器(不重置)，voucher_no/movement_type/领料日期改成逐页重新提取而不是"只取第一次"。修复后305条全部正确插入，两个凭证的行数和金额分别验证正确(23条/318.100 + 26条/1009.600 = 49条/1327.700，逐页人工核对源PDF数字一致)。
  - **本轮明确排除、未处理的相关发现**：`盘点/7月/澳大利亚六店2026年7月盘点表.pdf`——这是另一份完全不同结构的盘点快照(行号/物料编码/物料名称/库存数量/盘点数量/单位描述/单位编码/部门，8页，逐行单一部门，从前几行看覆盖范围明显比已导入的`附表2.xlsx`更广，包含刀具/手套/酒水等非食材库管物资)，跟`附表2.xlsx`是否有材料范围重叠、是不是同一次盘点事件的另一种导出格式，没有核实，留作独立待办，不在这批`internal_movement_import`范围内(这是流水单据表，那份文件是盘点快照，类别不同)。
  - 本地commit：见下方commit hash。**仍未push、未碰生产库。**
- [ ] `receiving_items`表接入现有收货App的实际提交流程——**2026-09-18新会话重新评估：这条待办按字面做不到**。打开`server/index.js`里`/api/receiving`实际代码发现，现有收货App从来没有采集过任何逐SKU数量数据——它是纯粹的验收清单勾选(冻品/鲜肉/称重/甜度测试等布尔项+照片)，前端`SupplierCard.tsx`的`selections`字段也证实是checklist布尔值，不是数量表单。而且更早一步：连最基础的`receiving_records`表(migration注释写着"数据库是权威源，飞书是同步副本")实际上**零写入路径**——现有代码只写飞书，从未INSERT过一行到Postgres，这个注释本身就是没跟上实际代码的过时说法。`receiving_items`(逐SKU结构化)没有任何现成数据源可以接。**更现实的路径**：`erp_receipt_items`(ERP入库单的逐项数据，人工从海底捞内部系统誊抄，本来设计上就该有条目级数据)是`stock_ledger`更可行的入库数据源，但ERP入库单目前也没有前端UI(录单工作台这次只做了Invoice流程)。是否要改造收货App去采集逐SKU数量(会改变门店同事在收货现场的实际操作方式)是产品/业务决策，没有擅自决定，留给用户判断；同时这次发现的`internal_movement_import`(见上一条)是另一个可能更现实的"出库方向真实数据源"候选，比等前端UI建成更快能用。
- [ ] 新增的`order_requests`/`stock_ledger`/`stocktake_records`同样还没接入任何路由/前端——目前只是表结构+视图，跟`receiving_items`当初的情况一样是"干净的预留表"，不是半成品；接入顺序留给用户决定
- [ ] 这批migration+清洗脚本只在本地commit(`33d7936`/`b3156cc`/`a257ec2`/`5c8ea4a`/`831fabb`)，**未push、未部署Render**——push前需要用户单独确认，同一贯的规矩
- [ ] 仍未做：炒料原料统计表.xlsx(需要新建BOM/配方表，独立设计工作)、7月发票校验总表的两份疑似文件(Coworkc/Cowrock那处发现，需要逐行核对)、盘点/目录下4-8月其余月份的正式盘点导出(这次只做了7月)、pending清单里16条记录(8月spreadsheet的9条+8月PDF的7条)的后续处理(等用户看完给方向)

以上"前端录单工作台UI"、"`receiving_items`接入"、"新表接入路由"均未动工；历史数据清洗第一批(发票+盘点)+第二批(7月/8月发票PDF OCR)已完成，具体实施顺序等用户决定。

- [x] **诊断并修复SKYJ(7月)/CFC(7月)两次OCR失败，过程中额外发现并修复一个影响全系统的真实bug**——2026-09-18完成。

**根因1(已修复，影响仅限这2份历史文件)：单据被扫描时多张拼进了一个PDF**。打开原始文件才发现`SKYJ 7月 Invoice.pdf`(8页)、`CFC 7月 Invoice.pdf`(20页)根本不是单张发票，而是把整月好几张独立发票(SKYJ 4张)、甚至一张Credit Note/Adjustment(CFC那份)扫描粘在一个文件里——这两份都是纯图片PDF(不是可提取文字的PDF)，用一次Claude API多页PDF理解调用(不是靠人工翻页数)逐页识别出"这一页属于哪张发票/调整单"的边界，人工核对过其中10页的截图跟API识别结果完全吻合。再用pymupdf按边界把原文件物理拆成16份子PDF，逐份复用现成的`parseInvoiceDocument`/`parseCreditDocument`，脚本`server/db/seed/import_p2_july_bundled_invoices_split.mjs`。

**根因2(已修复，影响整个OCR解析引擎，不只是这2份文件)：`ocrParsingService.js`的`max_tokens: 4096`对明细行多的发票不够用**。拆分后仍有10/16份报同样的"结构化结果缺少必要字段"，而且两次重试100%确定性失败(不是随机)——排除了图像质量问题(人工看过对应页面截图，内容清晰可辨)。用裸API调用(绕开现成封装，打印完整原始响应)确认：`stop_reason: "max_tokens"`，`output_tokens`精确等于4096上限，模型写完表头的`source_quotes`后没有token空间再生成`items`数组，导致`tool_use.input`里`items`键整个不存在——这正是触发`callExtractionTool()`完整性校验(`itemsLooksInvalid`)的真实原因，此前(包括7/17第四次深度复查那轮)一直被误判成"文档渲染问题"。实测验证：同一份文件(CFC 00094737，21行明细)，`max_tokens:4096`必定截断，改成`8192`必定完整返回。已把`server/services/ocrParsingService.js`的`max_tokens`从4096改成8192(改动带清楚的代码注释说明原因，不是拍脑袋改的数字，8192是实测样本的2倍余量)。**这个bug会影响生产环境所有明细行较多的发票/Credit解析，不限于这次处理的历史批次**——已经上线的`/api/invoices/:id/parse`路由在真实生产环境遇到多行发票时同样会触发，这是这次意外发现的、比"两份历史文件解析失败"重要得多的收获。

**额外发现的一个更隐蔽的问题(未造成数据损坏，但需要知道)**：CFC的发票`00094737`在9-17号原始7月批次里其实已经"成功"写入过一次(`invoices.id=169`，`created_at=2026-09-17`，`source_file_id`指向那份**未拆分**的20页原始文件)——说明当初面对20页混装的PDF，模型没有报错、也没有识别出"这是多张单据"，而是只老老实实读了第一张发票(00094737本身21行明细读得完整正确，已核对)、把其余9张发票(9~19页，约$41,175)当成看不懂的多余内容直接忽略。**这意味着原批次报的"CFC成功"是真的，但只覆盖了这个供应商7月发票的1/10，其余9张此前完全没有被系统看到过，也没有任何报错提示**——不是本轮批次找到的新bug造成的（本轮批次没碰这个id=169），而是本轮排查SKYJ时顺带发现的、原本会一直隐藏下去的历史数据缺口。

**最终结果（用户充值后已全部补跑完成）**：16份子文档全部成功入库——6份原批次就成功(CFC 00094914/00095005/00095097/00095192/00095383 共5张 + CFC 00095605 Credit Note 1张)，SKYJ全部4张在`max_tokens`修复后成功导入(id 207-210，36-37行明细/张)，CFC的00094737确认是9-17已有的完整记录(id=169，21行明细，不重复插入)，剩余CFC的5张发票(00094883/00095088/00095277/00095471/00095594)在用户于Anthropic Console充值后一次性补跑成功(id 211-215，17-22行明细/张)。独立SQL验证：CFC供应商`source='p2_historical_ocr'`共11张发票合计$29,995.57，SKYJ共4张合计$32,600.10。7月SKYJ/CFC的OCR数据缺口到此完全补齐，不再有pending项。

- [x] **系统性核查全部23个供应商，确认21个受同一问题影响，共150份单据缺口，已全部修复**——2026-09-18同一天完成。SKYJ/CFC不是个例：核查全部23个7月Invoice PDF的实际页数(Liquorland/福嘉本来就是1页)后发现，B&E(21页)/Coles(22页)/Cowrock(19页)/Kleanking(13页)/Beifang(13页)/老北方(12页)/张记(10页)等几乎全部多页文件都是"多张单据拼一个PDF"。用一次Claude API多页理解调用逐个识别页面边界(A&E/Clear ice/珍珠红三份文件第一次调用时模型报了明显错误的页数如90/307/159页，加一句"先数总页数"的提示后重跑修正为正确的1/4/2页——这是过程中发现的一个模型幻觉现象，不是文件本身的问题)，确认21个供应商总共150份单据(发票+Credit Note)分散在原批次只当成"1个供应商=1张发票"处理的文件里。
  - pymupdf按边界拆分成150份子PDF，脚本`server/db/seed/import_p2_july_remaining_bundled_invoices.mjs`复用`parseInvoiceDocument`/`parseCreditDocument`，按`(supplier_id,invoice_no)`/`(supplier_id,credit_note_no)`去重导入。
  - 结果：116张发票+11张Credit Note新增，22份确认是原批次已捕获的那一张(正确跳过不重复)，1份因临时网络中断("terminated")重试后成功，0份pending。
  - **7月invoices总数从24条(原批次)增加到147条，合计$189,285.67；credits从0条增加到12条，合计-$1,625.96**。逐供应商核实过金额合理性(见commit记录里的分供应商汇总表)。
  - 本地commit `6ef673f`，**已push**(纯数据回填脚本，不改schema/不改路由逻辑，风险等级判定为低，用户已授权"push也可以自己决定"，未额外确认)。
  - 张记文件里有4条页面识别结果落在"other"类型、doc_no格式是"业务凭证号 / INV-发票号"两个数字并列——判定按"/"后面的INV-xxxxx取真实发票号、类型按tax_invoice处理，理由是同一文件里其他行已经是清一色INV-xxxxx格式，格式一致不是编造。

- [x] **顺手完成：下载文件夹(Downloads)里AU6D相关的发票/毛利/入单文件整理进库管文件夹**——用户明确要求，2026-09-18完成。发现`Downloads/2026.7/`子目录虽然按供应商分类且用了SKYJ/A&E等熟悉的名字，但用图片核对收货地址后确认是**Sunnybank分店**的资料(不是AU6D/Brisbane CBD)，跟用户核实后确认原地不动、不并入库管。
  - 剩余约1098个散落文件，用两层判断：(1)文件名/发票号精确匹配到今天已核实的7月AU6D发票号(严格要求号码长度≥5位做子串匹配，短号码如Friendship的"1""2""3"完全不可靠、排除在外)；(2)文件名/内容明确写"CBD"/"Brisbane"/"六店"，或发票号是已确认AU6D供应商编号序列的自然延续(比如SKYJ的`26080941`紧接7月`26070841`之后)。
  - 移动165个文件到库管文件夹：新建`4月/`(2个)、`6月/`(2个)、`毛利报表/`(5个，细分毛利率表/菜品毛利率表/毛利分析报告，之前库管完全没有这类文件)、`9月/`(1个)；其余按供应商分别归入已有的`7月/2026.7.6d/<供应商>/`和`AU6D8月/<供应商>/`子目录(不是新建平级文件夹，第一版脚本手滑建错过，已手工纠正)。
  - 44个无法判断归属的文件(通用命名如"invoice.pdf"、没写门店的"Statement for Hai Di Lao Sydney Pty Ltd"、2025年的旧文件等)放进新建的`下载文件夹待分类/`，不强行归类。
  - 只做移动(move)，不删除任何文件；Downloads文件夹里跟发票/对账单/Credit Note相关的文件已清空(用关键词搜索验证)。

- [x] **系统性核查8月批次，确认同样存在"多单据拼一个PDF"问题，已修复+发现两个真实数据质量问题(供应商重复ID)需要用户判断**——2026-09-18同一天完成。8月比7月复杂：8月发票校验总表.xlsx此前已导入149条发票表头(有真实invoice_no，0条明细)，用三态逻辑处理：已有表头+0明细→补挂明细并用OCR结果补齐NULL字段(不覆盖已有值)；已有表头+已有明细→跳过；无表头→全新插入。脚本`server/db/seed/import_p2_august_bundled_invoices_split.mjs`，本地commit `cd65879`已push。
  - 162份子文档：74张全新发票+58条补挂明细+9张新Credit Note，17份已完整跳过，3份pending(FUJA/HAC/Sunstae供应商名不匹配，延续原批次已知问题，不是新发现)，1份网络中断重试后成功。
  - **真实发现并修复的bug**：张记的发票本身印了两个不同编号(单据凭证号+INV发票号)，拆分清单(manifest)层面的"other"类型清理逻辑生效了，但`parseInvoiceDocument`实际OCR读取时会独立把两个号拼在一起当invoice_no返回，绕过了manifest的清理——导致12条(7月4条+8月8条)invoice_no变成"235610 / INV-32792"这种复合字符串。逐条核查后发现其中1条(张记 INV-32792)是跟8月原批次(9-17号)已有正确记录完全重复的行(逐行核对明细品名/数量/金额一致)，已删除；其余11条清理invoice_no为正确的INV-xxxxx格式，清理前逐一确认过不会产生新的碰撞。
  - **发现但明确没有擅自处理、需要用户判断的问题**：系统性查了一遍8月全部发票号跨供应商精确匹配，发现两组供应商实际上在记录同一批真实发票：
    1. **Beifang(拼音"北方"的英文名) vs 北方**：8月发票校验总表用"Beifang"、这次OCR用的文件夹名是"北方"，两者是`suppliers`表里两个不同的供应商ID(SUP-005/SUP-034)，但14条发票号完全对应(仅Beifang一侧省略了"QLD"前缀，比如"10001218"对"QLD10001218")。
    2. **B&E vs BNE**：同样，供应商ID是SUP-003/SUP-004，10条发票号完全相同(无前缀差异)。
    3. **Coworkc vs Cowrock**：这组更值得注意——12条发票号完全相同，但P1项目此前(见[[project_kuguan_dual_ai_protocol]])的调查结论明确记录"这是两个真实不同的供应商，不是拼写变体"。这次的新证据(8月发票号逐条相同)直接跟那个结论矛盾。**没有擅自合并或判定哪个结论对**，需要用户看一下(可能是"两个供应商恰好这个月发票号一样"的巧合被此前调查误判，也可能真的是两个不同供应商，只是这个月的发票号巧合重叠——巧合重叠12次的概率极低，倾向于怀疑此前结论，但不能靠"概率上不太可能"就直接推翻已有的正式调查结论)。
    4. 如果Beifang/北方和B&E/BNE确认是同一供应商，8月发票总数里有约25条(14+10+1credit)是重复计入，需要合并/去重(哪个SKU作为保留ID、旧发票要不要迁移供应商ID，这类合并操作本身也需要走P1项目"供应商疑似合并"同款确认流程，不是这次批处理顺手能做的)。
  - 8月invoices最终230条，合计$299,706.46；credits 9条，合计-$982.95(这个数字包含上述可能重复计入的部分，等供应商合并问题解决后需要重新核算)。

- [x] **供应商重复ID问题已解决**——用户确认Beifang=北方、B&E=BNE、Coworkc=Cowrock均为同一真实供应商(Coworkc/Cowrock推翻了P1项目此前"两个真实不同供应商"的旧结论)后执行合并，本地开发库直接操作(生产库目前完全没有P2历史清洗数据，不涉及)：
  - 保留哪个供应商ID为准的依据：选历史使用范围更广的一侧(Beifang/B&E/Cowrock三个ID横跨7-9月都有数据，北方/BNE/Coworkc只在8月出现，是这次批量清洗新创建的重复)。
  - 逐条核对37对重复发票(14+11+12)的日期/金额/明细内容完全一致后，删除表头空(spreadsheet来源、0明细)的那一份，保留有完整明细(OCR来源)的那一份并把supplier_id改成保留ID；顺带把credits、supplier_delivery_schedule里指向被合并ID的记录也一并迁移。
  - 被合并的三个供应商(北方/BNE/Coworkc)标记`status='已合并停用'`+`merged_into_id`指向保留ID，不删除供应商记录本身。
  - **顺带发现并修正一个真实的日期解析bug**：BNE的发票`7081505`被OCR读成`2026-07-08`，但同批其他发票号按顺序都是8月(7057921=8/3,7093079=8/10...)，判定是源文档日期"08/07/2026"被误按MM/DD解析成7月8日，实际应为8月7日(DD/MM，跟B&E一侧spreadsheet记录的日期一致)——已改正，但这只是发现的個案，没有系统性排查整个OCR流程是否还有其他被误读的DD/MM日期，值得记一笔留意。
  - **顺带清理**：发现2条明显的测试数据残留(`invoice_no=TEST-9001`，B&E/BNE各1条，内容是"Frozen Prawns"这种测试占位数据，早期开发测试阶段留下、从未清理)，已删除。
  - 最终结果：不再有任何`(supplier_id, invoice_no)`重复；8月invoices从230条降到194条，合计$206,727.55；7月不受影响，仍是147条$189,285.67(北方/BNE/Coworkc这次的重复问题只出现在8月批次)。

- [x] **8月pending清单里的Sunstae/FUJA用发票号证据解决，HAC确认无解**：Sunstae文件夹的发票号"01965"跟已有供应商"Sunstate"(拼写差一个字母)同月发票号完全一致，用户确认合并；FUJA文件夹发票号"INV-000166"格式跟"福嘉"(SUP-043)7月的"INV-000115"序列延续，而名字更像的"福嘉FUJA"(SUP-044)完全没有历史发票记录，用户确认归入"福嘉"。两个都导入后发现Sunstate又产生了跟Beifang/BNE/Coworkc同样模式的空表头重复(发票号格式不同：spreadsheet记的"01965" vs OCR读到的完整"INV/26-27/01965")，核对日期金额一致后删除空表头那条。福嘉没有重复(7月/8月本来就是两张不同发票)。**HAC没有任何候选供应商匹配，维持pending，需要用户提供真实信息**。8月invoices最终195条，合计$207,805.57。

- [x] **系统性排查发票日期字段的DD/MM误读问题**：按供应商把invoice_no数字部分排序后找日期倒退的异常点(发票号基本按时间递增分配，倒退说明日期字段可能有问题)，找到10处候选，逐一打开原始PDF核实(不靠推算直接改数据库)：
  - **确认是真实bug并已修正(4处)**：Cowrock 024619(误读成2026-03-07，原文印着"03/07/26"=DD/MM，应为2026-07-03)、Cowrock 024905(误读成2028年，原文是2026年，应为2026-07-15)、Funglea 1523436(误读成2022-07-26，原文印着"22/07/26"，应为2026-07-22)、Yakult 000167350(误读成2026-01-09，原文是"01-09-2026"系统时间戳格式=9月1日，应为2026-09-01)。
  - **确认是供应商自己单据的错误，不是OCR问题，维持原样(1处)**：HTC HSIN-319930，原文清清楚楚印着"Date 7/08/2025"——供应商自己把年份打错了(应该是2026年的8月发票)，OCR如实转录了这个错误，没有改动(如实转录原文是这个流程的既定原则，供应商自己的错误不该由我们静默"纠正"掉，除非用户决定要在业务层面调整)。
  - **排查后确认不是bug，维持原样(2处)**：Coles两张发票(264768050/266144737)日期在原文里明确写着"12 July 2026"/"05 July 2026"，完全无歧义；表面看起来"发票号越大日期越早"的异常，是因为Coles的线上订单系统里不同账号(公司账号 vs 员工个人邮箱下单)下单顺序跟发票号分配顺序本来就不是严格对应，不是数据错误。
  - **顺带发现并修正一个独立的分类错误**：Cowrock的一条Credit Note(`invoice_no=240826`，`invoice_type='Credit Note'`，金额-158.95)在9-17号早期批次里被误插进了`invoices`表(该表本该只放正式发票)，而这次8月批次已经正确地把同一张Credit Note插入了`credits`表(id=23，还带着完整的"reason"字段说明退货原因)。核实两条记录内容完全对应同一张单据后，删除了`invoices`表里那条不完整的历史误插记录。
  - 未逐一核实的候选(Reward/明发的2处倒退，信号较弱)：暂未验证，判断优先级低于已确认的这几处，留作后续如果有空可以再查。
  - **这次排查没有系统性检查全部invoices记录的日期字段**，只检查了"按发票号排序后日期倒退"这个特征能筛出的候选——这个方法有盲区(比如日期错误后恰好仍然落在合理顺序里，就不会被这个方法发现)，不代表数据库里的日期字段已经100%可信，只是把目前能用低成本方法找到的明显异常都核实过了。
  - 8月invoices最终194条，合计$207,964.52。

- [x] **HAC供应商身份已由用户确认(独立供应商，不是任何已有供应商的别名)**：新建`SUP-048 HAC`，补跑pending发票(BR2985500，2026-08-18，$540，1行明细)。至此7月/8月批次的全部pending项清零。8月invoices最终195条，合计$208,504.52。

本地commit：见下方commit hash。**仍未push、未碰生产库**（`ocrParsingService.js`的max_tokens修复虽然是纯技术性bug修复且已充分验证，push会让这个改动影响生产环境的`/api/invoices/:id/parse`路由，仍然按规矩等用户确认后再push）。
